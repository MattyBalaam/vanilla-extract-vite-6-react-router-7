- install with npm install
- `$ npm run dev`
- should show import error
- uncomment ` // noExternal: ['@vanilla-extract/sprinkles'] in `vite.config.ts`
- should run

Will also work if you downgrade to Vite 5.4.14
